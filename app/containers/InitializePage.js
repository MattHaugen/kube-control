// @flow
import React, { Component } from 'react';
import Initialize from '../components/Initialize/Initialize';

type Props = {};

export default class InitializePage extends Component<Props> {
  props: Props;

  render() {
    return (
      <Initialize />
    );
  }
}
